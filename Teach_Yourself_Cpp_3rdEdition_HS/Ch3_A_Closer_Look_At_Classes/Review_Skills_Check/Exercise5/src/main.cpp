
/*
	Ex 5.: Give two possible restrictions to in-line functions.
*/

An 'inline' function must be defined before it is first used. Other common restrictions include:
 - it cannot contain loops
 - it cannot be recursive
 - it cannot contain 'goto' or 'switch' statements
 - it cannot contain 'static' variables. 
