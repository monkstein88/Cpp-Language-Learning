
/*
	Exercise 2.: What is a pure virtual function? If a class declaration contains a pure virtual function, what is that class called, and
  what restrictions apply to its usage?
*/

/*

	My answer:
		- A pure virtual function is a type of virtual function, that when declared in its base class, its function prototype is set to equal
    to 0 (=0). A class that contains a pure virtual function, is called an 'abstract' class. The restriction is that the pure virtual
    function in the base has to be overridden in the derived class afterwards, otherwise, the program will not compile.

	Official answer:
	 	- NONE

*/
