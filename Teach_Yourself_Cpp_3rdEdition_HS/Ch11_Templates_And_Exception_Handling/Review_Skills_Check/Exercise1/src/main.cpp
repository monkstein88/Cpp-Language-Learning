
/*
	Exercise 1.:What is a virtual function?
*/

/*

	My answer:
		- A virtual function is a kind of a placeholder for an actual function to take its place when the base in which the virtual funciton
		had been declared, is inhertied by a derived class, in which on its side overrides the base's virtual function, and when called from the
		appropriate class or class address.

	Official answer:
	 	- A virtual function is a function that is declared as virtual by the base class and then overridden by a derived class. A base class
		that contains at least one pure virtual function is called an 'abstract' class. But a class that contains at least one virtual function
		(in general), is called a 'polymorphic' class.

*/
