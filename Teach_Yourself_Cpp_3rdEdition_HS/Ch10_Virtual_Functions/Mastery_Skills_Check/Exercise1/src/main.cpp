
/*
	Exercise 1.: What is a virtual function?
*/


/*
	My answer:

		- A virtual function, is a class member function, that is declared (and possibly defined) in a base class, that is later
		  supposed to be inherited and overriden (redefined) by one or subsequent derived classes. It is a function must carry the
		  same function name, type, order and number of parameters, throughout its inherited.
		  It allows programs to achieve run-time polymorphism.

	Official answer:

		-  A virtual function is essentially a placeholder function that is declared in a base class and that is redefined by a
		   class derived from that base. The process of redefinition is called overriding. 

*/
