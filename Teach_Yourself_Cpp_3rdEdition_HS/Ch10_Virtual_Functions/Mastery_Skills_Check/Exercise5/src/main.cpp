
/*
	Exercise 5.: What is an abstract class? What is a polymorphic class?
*/


/*
	My answer:

		- I would suspect that an abstract class, is a class that contains only pure virtual functions, usually a base class. While
		the polymorphic class is the class that later on inherits and overrides the (virtual) methods of the base class.

	Official answer:

		-   An abstract class is a base class that contains at least one pure virtual function. A polymorphic class is one that
		contains at least one virtual function.

*/
