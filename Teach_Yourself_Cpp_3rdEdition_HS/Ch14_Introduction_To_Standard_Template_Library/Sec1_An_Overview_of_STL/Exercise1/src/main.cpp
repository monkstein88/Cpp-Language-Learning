
/*
	Exercise 1.: As they relate to the STL, what are containers, algortighms and iterators?
*/

/*
	My answer:
		- 'Containers' are items/objects that hold within themselves other objects.
		- 'Algorithms' are the actions/functions that are invoked upon a container.
		- 'Iterator' are essentially pointers (indexes) that are used to cycle through objects (contents) in an array of objects.

	Official answer:
		-  A container is an object that holds other objects. An algorithm is a routine that acts upon the contents of containers. An
			iterator is similar to a pointer.
*/
