
/*
	Exercise 12.: When you are assigning the address of an overloaded function to a pointer, what is t that determines which
	version of the function is used?
*/

/*
	My answer:
		- What determines it is the way that the function pointer is declared originally - the function pointer , must match the function's
		paraters type, order and number. Also the return type.

	Official answer:
		-  When you are obtaining the address of an overloaded function, it is the type specification of the pointer that determines
		which function is used.
*/
