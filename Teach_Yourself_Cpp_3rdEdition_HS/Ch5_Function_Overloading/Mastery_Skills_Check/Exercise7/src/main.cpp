
/*
	Exercise 7.: Briefly describe a default argument.

	My answer:
		A default argument - is defining a value of one or more parameters adjacent to a function, given (assigned) to that parameter
		when the function is called without passing it a value for that specified parameter. That parameter will automatically be
		assume that value of the previously set 'default'. The default value needs to specified either in the function definition
		or function declaration (in the parameters list), but not in both.

	Official answer:
		A default argument is a value that is given to a function parameter when no corresponding
		argument appears when the function is called.

*/
