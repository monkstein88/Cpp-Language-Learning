
/*
	Exercise 3.: On your own, think about ways tin which an overloaded constructor function can be beneficial to your own
	programming tasks.
*/


/*
	My answers:
		It could help provide default parameters/initialization of a class (object) that at the time of its declaration, it is
		not known what values to assign it (how initialize it). Also contstrucotrs enable declarataion of arrays of objects
		which could be with initialized values or not. Also it provides a kind of 'self-adjusting' mechanicsm of the
		instanciated object, when being declared, depening on the type, numbers, etc. paramaters are being passed
		(during its declaration).
*/
