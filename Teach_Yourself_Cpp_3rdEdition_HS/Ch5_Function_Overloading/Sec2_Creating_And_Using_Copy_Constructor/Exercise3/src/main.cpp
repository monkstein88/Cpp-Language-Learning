
/*
	Exercise 3.: In your own words, explain the purpose of a copy constructor and how it differs from a normal constructor.

	My answers:
		A 'copy' constructor is getting invoked/called when an object (class) is being initialized, passed to a function or
		returned by	a function. The purpose is to protect or more exactly prevent the original object from another object,
		function or simply during some return operation,
		to be contaminated/affected by these things. A normal constructor is called during assignments or when object is created
		without being initialized.

	Official answer:
		A copy constructor is invoked when one object is used to initialize another. A normal constructor is called when an object
		is created.

*/
