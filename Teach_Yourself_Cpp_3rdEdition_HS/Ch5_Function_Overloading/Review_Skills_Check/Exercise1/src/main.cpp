
/*
	Exercise 1.: What is a reference? Give two important uses

    My answer:
      A reference, is sort of an implicit pointer, that functions as an alias that can be used to refer/call
      the original variable or constant that the reference is initiated (connected) to. Providing in this way,
      a direct access to the variable it is tied to. A variable/constant and its associated reference can be
      used interchangebly. Reference val, is simpler to use than an actual pointer, syntax wise is the same
      as using the orinal variable/cont


      - Useful for function parameters, where a function is called, and passed a variable, that you want a function
      to work on directly.
      -

	  Official answer:
      A reference is a special type of pointer that is automatically dereferenced and that can be
      used interchangeably with the object it is pointing to. There are three types of references:
      parameter references, independent reference, and references that are returned by functions.
*/
