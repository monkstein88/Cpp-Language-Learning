/*
	Exercise 1.: Describe the operation of 'typeid'.
*/

/*
	My answer:
		- It is an operator /function that is used to check and report of the type of an object (passed to it as an argument) at run-time of
		the program. Mostly is being used with polymorphic classes to determine their types. It has the facilities to extract (get the name) of
		the type it is applied to and also could be used to compare two types togetether.

	Official answer:

	 - The 'typeid' operator returns a reference to an object of the class type that contains the type information.

*/
