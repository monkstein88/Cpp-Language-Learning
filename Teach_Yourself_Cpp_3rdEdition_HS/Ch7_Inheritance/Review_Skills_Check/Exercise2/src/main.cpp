
/*
	Exercise 2.: Must an operator be overloaded relative to user-defined type, such as a class?

	My answer: (Maybe, I understood the question wrongfully :( )
		- No, not necessarily if the default behaviour/functionality of the original operator already provides what we need and does
		it in the correct way that we need to. Otherwise, we might need to overload the operator to provide the operator the proper
		mechanism of what we want for it.

	Official answer:
		-  Yes. You cannot overload an operator relative to one of C++’s built-in types.
*/
