
/*
	Example 5.: As explained, the original C++ library was defined in the global namespace (ref. to 1997). If you will be converting older
	C++ programs, you will need to either include a 'using namespace std' statement or qualify each reference to a library member with
	'std::'. This is especially important if you are replacing old '.h' header files with the new-style headers (ref. to 1997). Remember,
	the old '.h' headers put their contents into the global namespace. The new-style headers put their contnets into the 'std' namespace.

	(Note: There is no code/program with this example.)
*/
