/*
	Exercise 5.: What does a conversion function do?
*/
/*
	My answer:
		- a conversion function is type of member function defined within a class, which the user specifies himself. The point of which is
		to convert an object (its call within a statement) into a value of type, which relates to the statement/call's context. with in the
		 object of that class is called, and convert that object for that
		call instance,  that had he  self overload that converts an object (or call to the object). It can is some cases be applied instead of operator overloading where it can be less of a messy solution,
		when trying to overload operators for different data types towards which it is to be applied.


	Official answer:
		-  A conversion function simply converts an object into a value compatible with another type. Conversion functions are typically used
		to convert objects into values compatible with the built-in data types.

*/
