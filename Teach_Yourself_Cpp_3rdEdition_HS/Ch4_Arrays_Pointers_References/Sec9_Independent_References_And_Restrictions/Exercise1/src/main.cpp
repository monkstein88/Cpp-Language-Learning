
/*
	Exercise 1.: On your own, try to think of a good use for an independent reference.

	My answer:
		Maybe for the use as an alias for a variable, which different refence names will be suitable in the
		use case (scenario) it is to be used. Thus, giving a variable/constant several different 'names',
		each of which could be suitibe and utilized depending on the 'context' it is put into to.
		 I.e. different functions could call different variable names inside themselves, but all these
		 variable names are references, which look up/relate to a single (common) variable.
		 This could be useful for debugging puproses.
*/
