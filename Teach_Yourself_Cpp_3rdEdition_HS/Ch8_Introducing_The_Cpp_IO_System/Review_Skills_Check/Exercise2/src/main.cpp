
/*
	Exercise 2.: What is 'protected' used for?
*/

/*
   My answer:
      - 'protected' is a keyword, a type of an access specifier, which could be used :
          = When used upon individual class members, it is used to specify the access for that member. And its behaviour of how
          it will be inherited, if its adjacent class gets inherited
          = When defining the a class, at the portion where tell if the is going to inherit (be derived of) another class (base).
          Which in turn would tell/set, how the base members are to be inherited to the derived class.


   Official answer:
      - The protected access specifier causes a class member to be private to its class but still accessible by any derived class.
*/
