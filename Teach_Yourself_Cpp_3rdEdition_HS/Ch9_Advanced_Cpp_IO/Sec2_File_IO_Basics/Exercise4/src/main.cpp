
/*
	Exercise 4.: What does 'is_open()' do?
*/

/*
	My answer:
		- It is a member function, that is called upon a stream (usually file streams) that we have declared (or had been declared
		already by the system), with which we can check if they are already associated/linked/initialized with a file (or a device).

	Official answer:
		- The is open() function returns true if the invoking stream is linked to an open file.
*/
