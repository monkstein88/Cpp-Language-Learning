
/*
	Intro 1.: In the preceding chapter, you learned how to overload the insertion and extraction operators relative to your own
	claseses. In that chapter, only console I/O was performed. However, because all C++ streams are the same, the same overloaded
	inserter function, for example, can be used to output to the screen or to a file with no changes whatsover. This is one of the
	most important and useful features of C++'s approach to I/O.

	As stated in the previous chapter, overloaded inserters and extractors, as well as I/O manipulators, can be used with any
	stream as long as they are written in a general manner. If you "hard-code" a specific stream into an I/O function, its use is,
	of course, limited to only that stream. This is why you were urged to generalize your I/O functions whenever possible.
*/
